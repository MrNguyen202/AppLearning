import { View, Text, ScrollView } from 'react-native'
import React from 'react'

const Search = () => {
  return (
    <ScrollView>
      <Text>search</Text>
    </ScrollView>
  )
}

export default Search